# Truestock

Project conventions, invariants, and working agreements live in `AGENTS.md` —
that file is the source of truth, read by Claude Code and by any other
agent tool that follows the `AGENTS.md` filename convention (Codex, etc.).

@AGENTS.md

If the `@` import above doesn't resolve, read `AGENTS.md` directly before
proceeding.
